export type SoundBoardItem = {
  id?: number;
  name: string;
  image: string;
  sound: string;
  quote: string;
};

export type SoundboardCollection = {
  id: number;
  name: string;
  image: string;
};
